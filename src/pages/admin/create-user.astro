---
import Layout from '../../layouts/Layout.astro';
import CreateUserForm from '../../components/CreateUserForm.tsx';

// In a real app, add authentication/authorization checks here
// to ensure only Account Admins can access this page.
// Also, the accountId should likely come from the user's session or URL.
const accountId = "account_abc"; // Placeholder
---

<Layout title="Account Admin - Create User">
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold text-center mb-8 text-gray-800">Account Admin Panel</h1>
    <CreateUserForm accountId={accountId} client:load />
  </main>
</Layout>
